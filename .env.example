# 使い方メモ
# 1) このファイルを .env という名前でコピー
# 2) WEBHOOK_URL を自分のDiscord Webhook URLに置き換える
# 3) python ocrStart.py で起動
#
# 例:
# WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/xxxxx
# LINE_WINDOW_TITLE=LINE
# OCR_LANG=jpn+eng
# POLL_SEC=10
# PREPROCESS=true
# THRESHOLD=160
# ADD_TIMESTAMP=true
# SAVE_SCREENSHOT_ONCE=true
# SCREENSHOT_DIR=screenshots

# Discord webhook URL
WEBHOOK_URL=

# LINE window title (partial match ok)
LINE_WINDOW_TITLE=LINE

# OCR language
OCR_LANG=jpn+eng

# Poll interval seconds
POLL_SEC=10

# Optional crop rect relative to window: left,top,right,bottom
# CROP_RECT=100,200,900,900

# Keep line breaks in OCR result (true/false)
KEEP_NEWLINES=false

# Preprocess image for OCR (true/false)
PREPROCESS=true

# Threshold for binarization (0-255)
THRESHOLD=160

# Add timestamp header (true/false)
ADD_TIMESTAMP=true

# Save screenshots for crop tuning
# SAVE_SCREENSHOT=true
# SAVE_SCREENSHOT_ONCE=true
# SCREENSHOT_DIR=screenshots

# Stop file (create this file to stop the loop safely)
STOP_FILE=STOP

# PID / status files for GUI controller
PID_FILE=ocr.pid
STATUS_FILE=ocr.status
HEARTBEAT_SEC=5

# GUI log display (latest OCR text)
LOG_FILE=ocr.latest.txt
LOG_MAX_CHARS=800

# OCR tuning
OCR_SCALE=1.0
MEDIAN_SIZE=0
SHARPEN=false

# Layout OCR (TSV output)
SAVE_LAYOUT_TSV=false
LAYOUT_TSV_DIR=layout_tsv

# Recent OCR log
RECENT_LOG_FILE=ocr.recent.txt
RECENT_LOG_LINES=200

# Discord webhook log
WEBHOOK_LOG_FILE=ocr.webhook.txt
WEBHOOK_LOG_LINES=200

# OCR tuning (recommended baseline)
INVERT=true
PSM=6
