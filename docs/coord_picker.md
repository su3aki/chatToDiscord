# coord_picker.py

## 目的
CROP_RECT の座標をクリックで取得します。

## 使い方
1. スクショ画像を用意
2. コマンドを実行
   `python coord_picker.py <画像パス>`
3. チャット欄の左上、右下をクリック
4. ターミナルに `CROP_RECT=...` が表示

## 例
`python coord_picker.py screenshots\line_full_YYYYMMDD_HHMMSS.png`
